# 发布文章流程

## 每次发文只改两处

1. 复制 `posts/article-template.md`，改名为 `YYYY-MM-DD-英文短标题.md`。
2. 修改 Markdown 顶部信息和正文。
3. 在 `static/js/article-index.js` 最前面增加一条文章索引。
4. 在 IDEA 的 Git 面板提交并推送到 `master`，GitHub Pages 会自动把 Markdown 渲染成文章页。

## 网页发布与修改（已部署 CloudBase 后）

访问不在导航中的 `publish.html`，输入发布密码后填写标题、英文短标题、摘要、分类、标签和 Markdown 正文即可。

云函数会自动完成：

1. 生成带 Front Matter 的 `posts/YYYY-MM-DD-slug.md`。
2. 在 `static/js/article-index.js` 顶部插入索引。
3. 将两项改动作为同一个 commit 提交到 `master`。

发布成功后等待 GitHub Pages 构建完成，再访问返回的文章链接。浏览器草稿仅保存在当前设备，不会上传。

部署前先完成 [CloudBase 文章发布功能部署.md](CloudBase文章发布功能部署.md)。

### 修改已发布文章

1. 登录后选择“修改或删除”，从文章列表选中一篇并点击“载入文章”。
2. 可以修改标题、摘要、分类、标签和 Markdown 正文。
3. 日期和英文短标题会锁定，以保证原有文章链接不变。
4. 点击“保存修改”，Markdown 和索引会作为同一个 `docs: update ...` commit 提交。

如果这篇文章或文章索引已被 IDEA、GitHub 网页端或另一台设备修改，页面会提示重新载入。重新载入后再保存即可。

同一个文章链接只能对应一条索引。若多条索引共用了同一个 `url`，发布页不会把它列为可编辑项，避免误改或误删同一份 Markdown；先在 IDEA 中将这些索引修正为各自真实文章链接。

### 删除文章

1. 先按“修改已发布文章”的步骤载入目标文章。
2. 点击“删除文章”，确认后系统会从索引和仓库中删除对应 Markdown，并创建一个 `docs: delete ...` commit。
3. 等待 GitHub Pages 构建完成后，文章会从首页、分类、归档和搜索中消失。

删除不是物理不可恢复：到 GitHub 的提交历史中 Revert 对应的删除 commit，即可把 Markdown 和索引一起恢复。

## Markdown 顶部信息

每篇文章开头保留这段结构：

```yaml
---
layout: article
title: "文章标题"
description: "首页和搜索结果使用的摘要"
date: 2026-08-26
category: "Java 后端"
tags:
  - "Spring Boot"
  - "事务"
permalink: /posts/2026-08-26-your-slug.html
---
```

`permalink` 与索引中的 `url` 必须一致。发布后，Markdown 本身不会以原始文本展示，GitHub Pages 会生成对应的 `.html` 文章页。

从 `article-template.md` 复制时，删除 `published: false`；它是模板的防误发开关，保留时文章不会生成线上页面。

## 索引示例

在 `static/js/article-index.js` 数组的最前面增加：

```js
{
  id: 'pay-order-ddd',
  title: '支付订单服务的 DDD 改造',
  url: 'posts/2026-08-26-pay-order-ddd.html',
  publishedAt: '2026-08-26',
  category: '项目复盘',
  tags: ['支付中台', 'DDD', '领域事件'],
  summary: '从传统三层服务到支付订单聚合的职责划分。'
},
```

## 图片

图片放入 `posts/assets/`。例如文章里引用：

```md
![流程图](assets/pay-order-flow.png)
```

## 写作规则

- 正文用 `##` 作为一级章节、`###` 作为章节内小节；不要重复写与 `title` 相同的 `#` 标题。
- 标题、表格、列表、代码块、引用块这些块级内容的前后各留一行空白。
- 表格的表头和分隔行必须连续；单元格中出现 Java 泛型、SQL、JSON、`|` 等代码时，用反引号包住。字面量 `|` 写成 `\|`。
- 无序列表使用 `-`，有序列表使用 `1.`；每个条目单独一行，列表结束后留一行空白。
- 代码使用 fenced code block，并写小写语言名，例如 ` ```java `、` ```sql `。

博客线上由 GitHub Pages 的 Jekyll/Kramdown（GFM）渲染。不要用 `file:///.../posts/xxx.html` 预览文章页，它不会执行 Jekyll 构建；推送后访问线上文章链接才是最终效果。

## 提交前检查

- `date`、`publishedAt`、`permalink` 和索引 `url` 是否一致。
- 索引是否放在数组最前面，保证首页和归档按最新文章展示。
- 分类是否属于长期分类；细分技术词放到 `tags`。
- 没有客户数据、公司内部地址、账号密码、Key、Token、手机号或其他敏感内容。
- 不要把 `article-template.md` 加入文章索引。
