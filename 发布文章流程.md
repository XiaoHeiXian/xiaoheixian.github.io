# 发布文章流程

## 每次发文只改两处

1. 复制 `posts/article-template.md`，改名为 `YYYY-MM-DD-英文短标题.md`。
2. 修改 Markdown 顶部信息和正文。
3. 在 `static/js/article-index.js` 最前面增加一条文章索引。
4. 在 IDEA 的 Git 面板提交并推送到 `master`，GitHub Pages 会自动把 Markdown 渲染成文章页。

## Markdown 顶部信息

每篇文章开头保留这段结构：

```yaml
---
layout: article
title: "文章标题"
description: "首页和搜索结果使用的摘要"
date: 2026-08-26
category: "Java 后端"
tags:
  - "Spring Boot"
  - "事务"
permalink: /posts/2026-08-26-your-slug.html
---
```

`permalink` 与索引中的 `url` 必须一致。发布后，Markdown 本身不会以原始文本展示，GitHub Pages 会生成对应的 `.html` 文章页。

## 索引示例

在 `static/js/article-index.js` 数组的最前面增加：

```js
{
  id: 'pay-order-ddd',
  title: '支付订单服务的 DDD 改造',
  url: 'posts/2026-08-26-pay-order-ddd.html',
  publishedAt: '2026-08-26',
  category: '项目复盘',
  tags: ['支付中台', 'DDD', '领域事件'],
  summary: '从传统三层服务到支付订单聚合的职责划分。'
},
```

## 图片

图片放入 `posts/assets/`。例如文章里引用：

```md
![流程图](assets/pay-order-flow.png)
```

## 提交前检查

- `date`、`publishedAt`、`permalink` 和索引 `url` 是否一致。
- 索引是否放在数组最前面，保证首页和归档按最新文章展示。
- 分类是否属于长期分类；细分技术词放到 `tags`。
- 没有客户数据、公司内部地址、账号密码、Key、Token、手机号或其他敏感内容。
- 不要把 `article-template.md` 加入文章索引。
